name: koborin_ai_infra
description: TerraDart infrastructure for koborin.ai (Dart → Terraform).
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  terradart_core: ^0.12.3
  terradart_google: ^0.12.3

dev_dependencies:
  lints: ^6.0.0
