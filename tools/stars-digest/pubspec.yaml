name: stars_digest
description: Generates the daily koborin.ai/stars personalized OSS newsletter.
publish_to: none
environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.13.2
  genkit_vertexai: ^0.2.7
  schemantic: ^0.1.3
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
  build_runner: ^2.4.0
  checks: ^0.3.1
